# 四份技术文档 一致性与可行性审查报告

> 审查对象（`docs/`）：`00-接口契约(1).md`（v1.0.2 冻结）· `01-DSH端插件实施方案(1).md`（v1.1）· `02-服务端实施方案(1).md`（v1.1）· `03-APP端实施方案(1).md`（v1.1）
>
> 方法：① 逐份通读并交叉比对；② 把文档中**关于 DSH 内部机制的每一条断言**拿到真实实现里核对（`D:\npm-global\node_modules\@deepseek-ai\dsh\`，即当前运行的这一份 DSH）；③ 核对文档引用的外部权威结论（coturn / Caddy / werift API）；④ 另起两路独立审计（跨文档矛盾、契约内部自洽）并与本报告合并；⑤ 对合并进来的结论逐条回原文复核，并对存疑项自行修正（下文标注了与本报告判断不同的两处）。
>
> **修订 r2（据文档作者的反驳）**：作者独立复验后指出若干处需校准，其中 **3 处本报告认账并已回改**——① §1 第 19 条原判 ✅ 属**假阳性**（只验了 exports 的"形状"、没验其"必需性"；实测 `dsh-app-boot` 的 bundle 解析**不依赖 exports**）；② M-9 的算例（4 条流占满 8 MiB）**与单流 256 KiB 信用上限矛盾、且与本报告 M-8 自相矛盾**，已改为 ≈32 条流并补上"单一有序 DataChannel 的 SCTP 发送队列"这一更贴近的机制；③ M-15 的机制写错（`bye` **丢失** ≠ 服务端不认 App 的 `bye`）。另有 2 项据其补充新增：**`sig` 自身的编码**（DER vs P1363，初版漏项）并入 B-3；**M-20**（10 分钟宽限期 ⇒ PC 离线超 10 分钟即需重扫码）为新增条目。反向地，本报告对作者反驳中的 3 处说法也做了校准（见 §5 末"对作者反驳的再校准"）。
>
> **修订 r3（与作者第二轮往来后收敛）**：① **M-9 与 M-8 的算例之争已合并**——两者指不同的量（信用窗口 vs 发送队列积压），且真正缺的是"契约**从未约束发送方队列**"，故直接采用与信用语义无关的修法（见 M-9 注）；② **M-11 定案改用第三种修法**：WebRTC `connectionState`/`iceConnectionState` 作主触发、15 s/45 s 降为兜底；本报告 r2 提出的"降阈值会涨 7.5 倍保活流量"**成本论证已撤回**（绝对量约 7 MB/天 ≈ 配额 3%），并补上"ICE `failed` 受 consent 30 s 约束、且覆盖不了 HOL 饿死"两条边界与"13–26 s 前端自愈窗口"的交叉约束；③ **M-20 补入 T=24h + LRU 淘汰**的建议（T 的最终值仍属用户决策）；④ 本报告 §1 第 19 条已按作者要求写明"两条 subpath exports 冗余可删，`exports['.']` 与 `main` **至少其一**"。**另：m-8 那一处本报告完全认账**——r2 里"对端回来后 presence 会驱动 App 重试 punch.request"是**未经文本支持的推断**（pair 销毁后 Bridge 已无"手机↔该 agent"的关联，`presence` 无对象可推），收敛为"**销毁时必须显式向仍在线的一端推 `PAIR_NOT_FOUND`**"。

---

## 0. 总体结论

| 维度 | 结论 |
|---|---|
| **技术路线可行性** | **可行**。DSH 侧共 **24 个**关键接触点被逐条核对，**全部为真**（§1），未发现"路线级"致命错误。 |
| **事实正确性** | **高**。对 DSH 内部行为的引用几乎全部与源码一致；契约变更日志 15 条中 14 条与正文吻合；coturn 限速参数与"never expiring"引文经官方文档核实为真。 |
| **矛盾与缺陷** | **4 条 BLOCKER、20 条 MAJOR、24 条 MINOR**（§2）。特征是**接口/机制口径与精度不足**，不是架构分歧；全部可在开工前修掉，且多数是"改一句话"的量级。 |
| **可判定性缺口** | 文档引用的 `../implementation/README.md`、`../../remote-link/remote-link-design-v0.4.md`、`../../remote-app-research/` **在本工作区及相邻目录均不存在**（实测），故 W1–W3、E5、§7 指标表无法核对。更关键的是：契约把最重要的恢复流程（"方案 B"）**定义在这份缺失的 v0.4 里**（见 M-11）。 |

**一句话**：方案成立；问题不在架构，而在于**契约的精度尚未达到"三端独立实现能一次对上"的程度**，同时两端方案里各有几处与契约字面不符的实现口径。其中 4 条 BLOCKER 分别会打破"免扫码"（B-1）、"蜂窝网下可用"（B-2）、"跨语言验签"（B-3）和"手机可被认证"（B-4）这四项已写进 DoD 的承诺。

---

## 1. 逐条核对：文档对 DSH 的断言 vs 实际代码

路径相对 `D:\npm-global\node_modules\@deepseek-ai\dsh\`，`node_modules/@deepseek-ai/` 简写为 `…/`。

| # | 文档断言 | 源码证据 | 判定 |
|---|---|---|---|
| 1 | `/api` Host 围栏**只校验主机名是否 loopback，不校验端口**（契约 §1、01 §7） | `…/dsh-client-connection/lib/index.js:94-98`、`:178-192` | ✅ 整条方案的地基成立 |
| 2 | cookie 名 = `dsh-auth-<base64url(sha256(authority))>` | 同上 `:200`、`:257-259`、`:382` | ✅ |
| 3 | authority = **含端口**的 Host（→ 改端口即全部 cookie 失效；PC 端口变则不影响） | `:229-238`、`:295` | ✅ 正是契约 §1"破坏性/非破坏性"二分的依据 |
| 4 | `GET /` 无有效 cookie → **401**（`authorizeIndex`） | `:363-402` | ✅ |
| 5 | token 交换须**恰好** `GET` + `pathname=="/"` + **恰好一个** token 参数；成功 → **303 + Set-Cookie** | `:369`、`:378-384` | ✅ 03 §3.4 与源码完全一致 |
| 6 | 改 Host：`/api*` → **403**；`GET /` → **401**；`/plugins/*` 与 dist → **200** | `:531`、`:532`、`…/dsh-host-frontend-static/lib/index.js:13`、`…/dsh-client-modules/lib/index.js:480-482` | ✅ 全部正确 |
| 7 | 组合 URL `/plugins/??<id1>/client.js,…` = `comboUrl()` | `…/dsh-client-modules/lib/index.js:182-183` | ✅（真实 URL 另带 `&rev=`） |
| 8 | 单个 `/plugins/<id>/client.js` 本机最大仅 **0.59 MiB** | 实测最大者 `dsh-client-ui-conversation/lib/client.js` = 620 160 B = **0.5914 MiB** | ✅ **与文档数字完全吻合**；48 个合计 3.62 MiB → E1 靶子成立 |
| 9 | `/api/remote.mux`：心跳 **2000ms**、连续 **2** 次未回 Pong → `terminate()`；**二进制帧 → `close(1003)`** | `…/dsh-api-gateway/lib/index.js:197`、`:253-266`、`:287-291`、`:398` | ✅ 契约 §3.2.2 精确正确 |
| 10 | `/api/remote.mux` 的**升级**也过同一围栏 | 同上 `:461-464` | ✅ |
| 11 | 前端退避 `500/×2/max=10s`；末档 `disconnected` 后**无限等待**，只有手动 `reconnect()`／网络变化／`stop()` 能唤醒 | `…/dsh-client-connection/lib/client.js:8-13`、`:75-83`、`:99-111`、`:139-146` | ✅ 与"13–26 s"吻合（源码区间 12.75–25.5 s） |
| 12 | `online`/`offline` 只在**系统网络**变化时触发，不能感知隧道断开 | `:4696-4706` | ✅ |
| 13 | web profile 默认 `patchReload: live`，监听 profile 与 home 两层 patch | `…/dsh-app-boot/lib/index.js:333-336`；`lib/profile-boot-*.js:110-117, 166-174` | ✅ |
| 14 | 装配失败**连带 DSH 启动失败**；`inject` 缺服务 → FIBER_PENDING 也计失败 | `…/dsh-app-boot/lib/index.js:1431-1459` | ✅ |
| 15 | fail-fast 自救：profile patch 加 `- id: mobile-link` + `disabled: true` | `:1437`、`:360-364`（模板注释明示 "id-targeted config overrides, **disables**"）、`profile-boot-*.js:141-147` | ✅ **指引有效，是官方机制** |
| 16 | `ctx.webServer.port` 可读真实端口 | `…/dsh-host-webserver/lib/index.js:163-167` | ✅ |
| 17 | 转发目标默认 `127.0.0.1:3080` | `…/dsh-web-app/cordis.patch.yml`（`host ?? '127.0.0.1'`、`port ?? 3080`） | ✅ |
| 18 | `dsh plugin --profile web add` 转发 pnpm；**无 pnpm → 127**；成功后 `reconcilePlugins` 写 `dsh.profile.bundles` | `lib/plugin-F7ZVfRyo.js:101-128`、`:46-78` | ✅ 精确正确 |
| 19 | bundle 形态 `dsh.bundle.patch` + exports 两条 | `…/dsh-base/package.json`（形状）；但 `…/dsh-app-boot/lib/index.js:800-813, 819, 861` 显示解析 bundle 目录**不依赖 exports**：`packageDirFromAnchor` 走 `createRequire(anchor).resolve.paths()` + `existsSync`，注释明写 "Resolution does not require the package to export `./package.json`"，manifest 与 patch 都用 `readFileSync(join(packageDir, …))` 绝对路径读盘 | ⚠️ **形状一致，但 01 §2 给出的"必需"理由不成立**：两条 subpath exports 是冗余（删掉安全），只有 `exports["."]`/`main` 必需。**初版此处误判为 ✅**（只验了形状、没验必需性），据作者反驳修正 |
| 20 | 启动行打印 `dsh web: <带 ?token= 的 URL>` | `…/dsh-web-app/lib/index.js:206-211` | ✅ |
| 21 | `approval/request` 是 **waterfall**，服务名 **`approval`** | `…/dsh-user-approval/lib/index.js:76`、`:179` | ✅ |
| 22 | `ctx.storage` 是 hub 非 KV；写记录走 `ctx.storageDomain` | `…/dsh-storage/lib/index.js:84-104`、`…/dsh-storage-domain/lib/index.js:46-64, 341` | ✅（细节见 m-21） |
| 23 | 浏览器会话密钥在 `$DSH_HOME/.credentials.yaml` | `…/dsh-credentials-local/lib/index.js:13-58`、`…/dsh-client-connection/lib/index.js:196` | ✅ |
| 24 | "设置→通用"有重连入口；DSH 有 `/export` 下载入口 | `…/dsh-client-ui-settings-general/lib/client.js:553-554`；`…/dsh-web-app/cordis.patch.yml`（`session-log-download`："`/export` command plus the shared download dialog"） | ✅ |

### 1.1 本方案最高风险项（DTLS 证书持久化 + 指纹 pin）：API 层已可排除

核对 `werift@0.24.4` 公开类型定义：`RTCConfiguration.certificates?: RTCCertificate[]`（可注入）；`RTCCertificate` 构造签名 = `(privateKeyPem, certPem, signatureHash)`（**可直接从持久化 PEM 载入**）；另有 `dtls.keys:{certPem,keyPem,signatureHash}`；`getFingerprints()` 返回 `{algorithm,value}[]`（`…/peerConnection.d.ts`、`…/transport/dtls.d.ts`）。

**结论：契约 §6 的"整证书持久化 → `fp` 稳定"在 werift 上可实现。** 建议把 W1 的风险从"证书能不能注入"收窄为"注入后与 org.webrtc 的握手与指纹协商是否一致"。

> **附带简化（承作者补充）**：QR 的 `fp` 可直接取 `getFingerprints()` 的输出，不必自己算 DER 的 sha256——少一个出错点。需在 W1 顺带确认 werift 的 `value` 是否就是"冒号分隔大写十六进制"（契约要求比对时大小写归一，故即使大小写不同也不影响 pin，只影响与契约措辞一致性）。注意这**只消除插件侧的计算风险，并不消除 m-5 的 §6 措辞缺陷**——手机侧仍必须"去掉算法前缀后再比对"。

### 1.2 引用的外部权威结论

| 引用 | 核验 |
|---|---|
| coturn 认证/限速（契约 §5、02 §4） | ✅ **全部属实**：REST-API 用户名/HMAC 形式、`max-bps`=单会话、`bps-capacity`=整机合计、`user-quota`=并发分配数、无原生按天配额、`?transport=tcp` 只需不设 `no-tcp` 且无需证书、"session password never expiring" 引文真实 |
| 02 §3.3/§5：Caddy `reverse_proxy` **默认追加** XFF，故"取第一个是错的" | ⚠️ **前提不准确**：Caddy 默认**忽略入站** `X-Forwarded-*` 以防伪造，仅在配置 `trusted_proxies` 后采信。结论（取**最右**）仍安全，但前提要改写（m-16） |

---

## 2. 矛盾与缺陷清单

判据：**BLOCKER** = 照两份文档实现会直接不通或破坏已承诺的产品语义；**MAJOR** = 真实分歧/精度不足，联调前必须拍板；**MINOR** = 措辞、边界精度、覆盖度。

### 2.1 BLOCKER（4 条）

#### B-1 插件"dispose 就发 `bye`"把 live 热重载变成"必须重新扫码"——正是 `boot_id` 要防的事

- 01 §2.2（`01:104`）：触发 Cordis fiber dispose 的事件包括 DSH 退出、把该行 `disabled`、**以及 web profile 的 `patchReload: live` 热重载**
- 01 §2.2 清理顺序（`01:108`）：**1. 先向 Bridge 发 `bye`**（契约 §4.1：显式撤销 → Bridge 立即销毁 pair）
- 01 DoD（`01:24`）：插件被 dispose 时**主动发 `bye`**
- 契约 §4.1（`00:98`）：`bye` = **显式撤销：任一端发出即立即销毁 pair，不等宽限期**
- 契约 §4.2（`00:105`）：`boot_id` 必须**进程级**…否则 live 热重载会被误判为"DSH 重启"，**逼用户重新扫码**

闭环：热重载 → dispose → 发 `bye` → **pair 立即销毁** → 同进程（`boot_id` 相同）重连得 `hello.ok{pair:null}` → 按 01 §4.4（`01:178`）重印新 QR → **用户必须重新扫码**。Bridge 侧已用 `boot_id` 规避"误判重启"，却被插件自己的清理逻辑抵消；01 §8 的 E10 断言① 对"热重载"这一类同样 `boot_id` 的事件将不成立。

**次生风险**：被踢的**旧连接** dispose 时也发 `bye`；契约与 02 都没把 `bye` 限定在"当前连接代次"，旧代次的 `bye` 可以杀掉刚挂载的新 pair。（02 §2.1 约束 2（`02:62`"pair 的所有权必须绑定当前连接的实例（连接代次）"）已给出正确原则，只是没套用到 `bye` 上。）

**修法**：① 01 只在**真正的进程退出**时发 `bye`，不随每次 fiber dispose 发；② 契约 §4.1/02 §3.2 给 `bye` 加"当前连接代次"条件，旧代次 `bye` 记为过期并忽略。建议两条都做。

> **① 不能写成硬保证**（承作者补充）：DSH 关闭走的是 root context 的**异步 drain**（`dsh-base/cordis.patch.yml` 注释："Every CLI exit path drains it by disposing the root on SIGINT/SIGTERM"），所以 disposer 里 `await` 一次 WS 发送比 `process.on('exit')` 的纯同步写更可靠；但硬杀、崩溃、网络中断仍会丢帧。**正确措辞是"尽力而为地发，丢了由 10 分钟宽限期兜底"**——写成"一定会撤销"是新的假承诺。
>
> **与 M-20 的耦合**：①依赖"10 分钟宽限内能恢复"，而 M-20 指出"PC 侧离线 >10 分钟就丢 pair"。热重载后若网络恰未恢复、重连耗时超过 10 分钟，一样要重扫码。两条必须一起改才算闭合。

#### B-2 APP 在"免扫码重连"路径上**拿不到任何 TURN/STUN 凭证**；契约侧同源缺陷：`relay.request` 不在任何流程里

- 契约 §4.1（`00:86`）：`hello.ok` 载荷 = `pair:{pair_id,agent_id}|null`, `peer_online`, `ts` —— **不含 TURN 块**；只有 `pair.ok`（`00:90`）带 `turn:{...}`
- 03 §3.1（`03:55`）：`iceServers 来自 pair.ok.turn`…**单阶段 ICE——首次 gather 即含 relay 候选**
- 03 §3.3（`03:77`）：`App 重启 → hello → hello.ok{pair 非空} → punch.request → offer/answer 重建（免扫码）`
- 03 §3.3 存储清单（`03:75`）：只存 `sigURL`、`agentId`、`authority`、设备公钥别名
- 契约 §4.1（`00:96`）：`relay.request` | **双→Bridge** | 请求 TURN 凭证（**为将要发生的新 gather 准备**）
- 契约 §5（`00:156`）：…"（见 §4.2 的插件主动重发 `punch.offer`）" —— 而 **§4.2 正文（`00:101-130`）从未出现 `relay.request`/`relay.alloc`**，该交叉引用是悬空的
- 全文检索：**03 全文 0 次**提到 `relay.request`/`relay.alloc`（01 在 `01:182` 用到了）

结果：App 重启这条**最常见**路径上 `iceServers` 为空——连 STUN 都没有，契约 §5 专为"UDP 被封"保留的 `turn:…?transport=tcp` 手机侧兜底彻底失效（而蜂窝/企业网正是它的目标场景）。可用性上会退化为"仅 PC 侧 relay + 手机 host 候选"的单边中继：多数 CGNAT 下仍可能连通，因此**不是"一定连不上"，而是稳健性与契约承诺的候选结构都不成立，且失败时表现为"打洞失败"而非明确报错**——与 01 §4.4 自己警告的"静默缺少 relay 候选"同类。

**修法**：03 §3.3 补"**任何一次新 gather 之前**（含免扫码重连、网络切换、插件重发 offer 后的重建）若本地无有效凭证或剩余 TTL 低于阈值，先 `relay.request` → `relay.alloc` 再建 PeerConnection"；契约 §4.1/§4.2 补"MUST"并把 §5 的悬空交叉引用指向正确的流程。

#### B-3 `hello` 的**被签名串**未定义：`ts` 的单位/类型、`nonce` 的编码都没有规定

- 契约 §4.1（`00:85`）：`sig` = 设备私钥对字符串 `"hello|"+ts+"|"+nonce` 的签名（P-256/SHA-256）；Bridge 校验时间窗 ±60s、nonce 窗口内去重
- 同表已给 `role:"agent"|"app"`、`pub`(SPKI base64url)、`peer_online:bool` 等标了类型，**但 `ts`/`nonce`/`proto_ver` 未标**
- 01 §4.4（`01:177`）、03 §3.3（`03:74`）都照抄同一句 `"hello|"+ts+"|"+nonce`，同样无类型

签名是**对序列化后的字节串**做的，而这四个跨三种语言（JS / Go / Kotlin）的实现各自决定：`ts` 是秒还是毫秒？JSON number 还是字符串？Go 侧解成 `float64` 后用 `%v` 可能产出 `1.7577005e+09`；`nonce` 是 base64url 还是 hex、多少字节？**结果可以是"三端单测各自全绿、联调时 100% 验签失败"，且诊断信息只有一个不透明的签名错误**——即完全无法配对。±60s 的时间窗在单位不明时也失去意义。

**修法（一句话）**：写死"`ts` = epoch 秒（JSON number，十进制无小数）；`nonce` = 32B 随机 → base64url；被签名字节串为 UTF-8 `"hello|" + ts.toString(10) + "|" + nonce`"。严重度为 BLOCKER 仅因"一旦两端猜不一致，失败是全面且确定的"，修法本身极廉价。

> **必须同时钉死 `sig` 自身的编码**（承作者补充，**本报告初版漏项**）：契约给 `pub` 标了 "SPKI base64url"，却**没给 `sig` 任何类型标注**。而 ECDSA 签名有两种编码——DER 与 IEEE P1363（定长 r‖s，各 32B）；Node 的 `crypto.sign` 默认 DER，WebCrypto 的 ECDSA 产出 P1363，Java/Kotlin 的 `SHA256withECDSA` 产出 DER；外层还有 base64 与 base64url 之别。**同样是"三端单测各自全绿、联调 100% 验签失败"**。建议一并写死（如"P1363 定长 r‖s，各 32B，base64url"或"DER，base64url"，二选一）。

#### B-4 03 的 `hello` 缺 `pub`，手机身份无法建立

- 契约 §4.1（`00:85`）：`hello` | 双→Bridge | `role`, **`pub`(SPKI base64url)**, `boot_id`(agent 必填), `ts`, `nonce`, `sig`, `proto_ver`
- 03 §3.3（`03:74`）：WSS `hello{role:"app", ts, nonce, sig}` + `pair.bind{ptok}`（**身份已由 hello 确立**，bind 不再带密钥材料）

`pub` 是 Bridge 计算手机标识 `sha256(pub)` 并**验签**的唯一输入，而契约又明确 `pair.bind` 不带密钥材料。按字面实现必然 `AUTH_FAILED`。括号里那句"身份已由 hello 确立"成立的前提恰恰是 hello 里有 `pub`。判断为缩写漏写，但属 BLOCKER 级接口字段，必须显式改。

### 2.2 MAJOR（20 条）

| ID | 问题 | 关键证据 | 影响 / 修法 |
|---|---|---|---|
| M-1 | 手机侧不持久化 `fp`，且**未要求对每次 offer 重新 pin** | 契约 §6（`00:171`）红线要求每次 `setRemoteDescription` 前比对 **QR 中 `fp`**；03 §3.3 存储清单（`03:75`）**无 `fp`**；契约 §4.1（`00:94`）插件"可在**任意时刻**主动重发 `punch.offer`，手机必须随时接受并应答" | 免扫码重连恰是 **App 重启后**的场景，此时 QR 已不在手上 → pin 退化为"仅首次配对时校验"，而 pin 是防"Bridge 被攻破后中间人"的唯一屏障（契约 §6 明确 `agent_id`/`boot_id` 都不参与校验）；插件可**任意时刻**重发 offer 更放大了该窗口。**修法**：§6 + 03 存储补 `fp`，并写明"每一次 `setRemoteDescription` 前都 MUST 比对" |
| M-2 | offer 中**不存在** sha-256 行时无 fail-closed 规则 | 契约 §6（`00:171`）只规定"取 sha-256 那行、不得任取一行"，未规定"没有该行怎么办" | 宽松实现（"找不到就不比"）会静默关掉唯一 MITM 防线。**修法**：`00:171` 补"若不存在 sha-256 行 → MUST 立即中止，不得继续 setRemoteDescription" |
| M-3 | `DSML_AUTHORITY` 可配 vs 契约"只有一个固定值" | 契约 §1（`00:13`"不存在备用 authority，也不做运行时切换"）vs 01 §7（`01:222` 提供该环境变量）vs 03 §3.2（`03:64-65` 硬编码 13080 且**不做端口回退**） | 非默认值必然导致 OPEN 一致性断言失败 → `RST`，且 cookie 名不匹配 → 全部 401；契约 §1（`00:19`）"手机侧端口可以自由选择"更会误导。**修法**：删除该变量，或改为"值 ≠ 13080 时装配期 fail-fast" |
| M-4 | `proto_ver` 三端都没落实：契约要求、两端不发、Bridge 不校验、且无对应错误码 | 契约 §4.1/§7（`00:85`, `00:181-182`）；01（`01:177`）/03（`03:74`）均无该字段；02 §3.2（`02:87`）hello 处理无版本校验；§4.3（`00:136-145`）无版本码 | 版本门禁在三份实现里都不存在。更糟：若退回 `AUTH_FAILED`，契约规定客户端动作是"提示**重新扫码**"（`00:138`）——而重新扫码无法解决版本偏差，会形成死循环。**修法**：三端补齐 + 新增 `PROTO_VER_UNSUPPORTED`（动作=提示升级、禁止重试） |
| M-5 | 帧头 `flags` 无任何语义 | 契约 §3（`00:45-48`）是全文唯一出现处；§3.1/§3.2 均无规定 | 每个帧都带这个字节却无规则，三端可各选一种行为。**修法**："v1 恒为 0；发送方 MUST 置 0；接收方 MUST 忽略非 0 值" |
| M-6 | `payload ≤ 16384` 与"≤16 KiB 切帧 / `maxMessageSize` 保守值"的关系未定，**差 11 字节** | 契约 §3（`00:48`）与 §3.2.3（`00:68`） | 帧头 = 1+1+1+4+4 = **11 B**，故 payload 16384 的帧在 DataChannel 上是 **16395 B > 16384**；`maxMessageSize` 是**整条 SCTP 消息**的上限（这正是引用它的原因），"理由与数字自相矛盾 11 字节"。且 `len u32` 无上限规则 → 恶意 `len` 可致接收方按 4 GiB 分配。**修法**：二选一定死（`len ≤ 16372` 且总长 ≤16384，或 `len ≤16384` 且接收侧 MUST 接受 16395），并补 `len > 上限 → RST` |
| M-7 | WINDOW 的**信用额度数值**未定义 → 可致慢性死锁 | 契约 §3.1（`00:59`）"通告**新增**信用额度"；§3.2.4（`00:69`）"每写入 socket ≥64 KiB 回一个 WINDOW" | 触发条件明确、**数值不明确**："每次固定授予 64 KiB" 与 "授予自上次 WINDOW 以来写入的字节数" 都算忠于原文；前者每轮销毁（写入−64 KiB）的信用，从 256 KiB 起约 43 轮后发送方信用归零并**永久暂停读 socket**，健康流卡死直到 45 s 后整隧道被拆。另未说明 256 KiB 初始信用是**每方向**还是共享，`credit u32` 也无溢出/钳制规则。**修法**："WINDOW.credit MUST = 自上次 WINDOW 以来本流已写入 socket 的累计字节数；累计 ≥64 KiB 即 MUST 发送；初始信用为**每方向** 256 KiB" |
| M-8 | 无并发 stream 数上限 → 接收方内存上界不可推导 | 契约 §3.2.1（`00:66`）stream_id 由手机分配且**数量无上限**；§3.2.4（`00:69`）全局 8 MiB 仅为发送侧自我约束 | 接收方唯一上界是"每流 256 KiB + socket 缓冲"，8 MiB / 256 KiB = **32 条**即可打满全局上限（文档从未给出这个数），而 stream 数无上限 → 手机开 100 条流时接收方最坏 25 MiB/隧道，恰是该上限想防的事。契约也没给接收方可执行的约束。**修法**：规定"每隧道并发 stream 上限 N，超出 → RST"，接收方上界即为 N×256 KiB + 8 MiB |
| M-9 | 8 MiB 全局上限无公平性/优先级规则 → 可饿死承载会话的 mux WebSocket | 契约 §3.2.2（`00:67`，DSH 2s 心跳、2 次未回 Pong 即 `terminate()`）与 §3.2.4（`00:69` 全局 8 MiB 被所有流共享）；§3.2.3（`00:68`）切帧后直写 socket | 饿死是真实的，但**机制不是"4 条流占满 8 MiB 信用"**：单流信用上限 256 KiB，要堆到 8 MiB 需 **≈32 条流同时满信用**（与 M-8 的算术一致——**初版此处的 4 流算例有误，且与 M-8 自相矛盾**，据作者反驳修正）。更该注意的是**单一有序 DataChannel 的 SCTP 发送队列**：PONG 排在积压之后就必须等队列排空（2 Mbit/s 下 8 MiB ≈33.5 s），而 DSH 侧约 6 s 就 `terminate()`（见 M-11）→ 用户会话死亡，而每条流自身都"行为正确"。**修法**：不能只留信用额度——应按流限制 `bufferedAmount` 积压，并给控制帧独立优先通道（独立 DataChannel 或非有序控制通道），或把全局上限降到远低于最慢路径的带宽时延积。**注（r4 定稿，撤回 r3 的机制表述）**：**发送方队列并非无界**——信用回填必须以"接收方已收到并写入 socket"为前提，故对合规发送方有 `sent − received ≤ 信用窗口 ≈256 KiB`，队列同界（推导见下）。因此"一条大流即可堆满"**不成立**，要堆到 8 MiB 需 **≈32 条并发大流**（与 M-8 的并发上限缺失叠加）。**真正的缺陷是契约缺三件事**：①"在途"未定义**是否计入 `send()` 入队字节**；②发送方队列（`bufferedAmount`）的上界未写；③**控制帧是否豁免全局在途上限、如何在共享有序队列中不被 DATA 阻塞**——第三条才是"8 MiB 的 DATA 排在 PONG 前面、2 Mbit/s 下等 ≈33.5 s"的直接成因，也是 DSH 侧约 6 s `terminate()` 之前等不到 PONG 的原因。**修法**：写死①②（含"信用必须在**入队前**记账"这一 MUST）并按流限制 `bufferedAmount`；③给控制帧独立优先通道（独立 DataChannel 或非有序控制通道）。**注（与 M-8 合并）**：信用窗口（≈32 条流才够 8 MiB）与发送队列上界（同为 ≈256 KiB/流）**一致而非矛盾**，两者都不足以在单流下堆出 8 MiB；故不再争论算例，直接采用与信用语义无关的修法 |
| M-10 | 关闭/重置状态机缺失：重复 FIN、FIN 后 DATA、RST 与 FIN 竞争、未知 stream_id、未知 type | 契约 §3.1（`00:57-58`）、§3.2.6（`00:71`） | 尤其**半关闭语义未定义**：若接收方把 FIN 当整条 `close()` 处理，则"请求体以 FIN 结束、响应体随后回来"的 HTTP 事务会**静默截断响应体**；未知 type 也无处置规则（而信令层 `00:79` 是有的）。**修法**：补一张 6 行表（FIN 收到 → `shutdown(SHUT_WR)` 但仍继续读；二次 FIN → 忽略；自己 FIN 后仍可收 DATA/WINDOW；对端 FIN 后收 DATA → RST；未知/已释放 stream_id 的任何帧 → RST；未知 type → RST 该流而非整隧道） |
| M-11 | 45 s 隧道判死 vs ≈6 s 内层 WS 被杀；且唯一的恢复流程"方案 B"**定义在缺失的 v0.4 里** | 契约 §3.2.5（`00:70`"45s 未收到任何帧判隧道死亡 → 整条 DataChannel 销毁重建（方案 B，不做流内恢复）"）vs §3.2.2（`00:67`）；契约 §1（`00:3`"本文是…三方的**唯一**接口真相"） | 按源码推导：DSH 每 2 s ping，`missed≥2` 即 terminate → 最后一次 pong 之后**约 6 s**（t=2s 记 1 次、t=4s 记 2 次、t=6s 终止）就杀掉 mux WS，而隧道自己要到 **45 s** 才认为"死了"。于是存在 (≈6s, 45s] 的窗口：隧道自认健康、承载整个会话的 WS 已被 DSH 终止。"方案 B"仅点名未定义——谁发起重建（新 offer 还是 `punch.request`？）、是否需重新 offer/answer、pair/凭证/stream_id 空间是否复用、2 s 限流是否适用，全部空白，而契约自称是唯一真相。**修法（r3 定案）**：把 **WebRTC 自身的 `connectionState`/`iceConnectionState` 作为重建「主触发」，15 s/45 s 的 PING 降为「兜底」**（任一满足即销毁重建）。理由：ICE 层在 SCTP/DTLS **之下**，其 consent 检测**不受隧道内 HOL 积压影响**（而隧道自己的 PING/PONG 是 DataChannel 内帧，同样会被积压饿死——即被积压的隧道连"自己该判死"都检测不及时）；且 01 §4.4 与 03 §3.1（`03:60`）本就要求把 `onFailed`/`onDisconnected` 接进状态机，两端 API 均存在（werift：`connectionStateChange`/`iceConnectionStateChange` 事件 + 两个 getter + `RTCIceTransport.onStateChange`；org.webrtc：`PeerConnection.Observer.onConnectionChange`），缺的只是把该回调与 45 s 阈值的关系写清。**本报告原"降阈值会涨 7.5 倍保活流量"的成本论证已撤回**（绝对差约 7 MB/天，占 200 MB/天配额约 3%，非决策依据；且隧道几乎不空载——mux WS 本身每 2 s 就有帧）。

**必须同时写清的三条边界**：
1. **延迟预期要写实**：ICE 的 `failed` 由 consent 超时驱动（RFC 7675 为 30 s），`disconnected` 更早，故改进是"45 s → 约 ≤30 s"，**不是"数秒"**；werift 是否实现 consent 计时、`disconnected` 何时触发**必须在 W1 实测**（现有类型定义只证明状态与事件存在，不证明触发时机）。
2. **ICE 覆盖不了 HOL 饿死那一种故障**（链路健康、仅队列被占满），那一种只能靠 M-9 的修法（按流限 `bufferedAmount` + 控制帧独立通道）。
3. **与前端自愈窗口交叉**：DSH 前端自愈窗口只有 13–26 s（`…/dsh-client-connection/lib/client.js:99-146`）。若 W1 实测 ICE 仅在 ~30 s 才报 `failed`，隧道重建会**晚于前端放弃**，此时 03 §4 的 `webView.reload()` 是**必需路径**；若要在窗口内重建，可加"**有活动流时**收紧 PING（如 3 s 间隔 / ≈9 s 阈值）"——有活动流时隧道本就不空载，该收紧不会引入空载误判，也不改动无活动流时的 15 s/45 s。 |
| M-12 | stream_id 空间在隧道重建后的归属未定义，无隧道纪元 | 契约 §3.2.1（`00:66`"从 1 递增，u32 回绕前重连"）与 §3.2.5（`00:70` 整条重建） | u32 回绕本身是 50 天量级的小事，真正的问题是**重建很常见**（网络切换）：若手机重建后从 1 重新编号，而插件仍持有上一incarnation 的半关闭 stream 1，就会出现"新 OPEN 被 RST"或"旧 WINDOW 记到新流上"。**修法**：加 1 字段隧道纪元，或明确规定"重建后 stream_id 从 1 重新开始，两端 MUST 丢弃全部旧流状态" |
| M-13 | "pair 销毁只有三个触发条件"被替换流程证伪：实为**四个**；且变更日志声称的修复与正文不符 | 契约 §4.2（`00:107`"只有三个"）vs `00:128`"新设备替换：手机 B 对同一 agent `pair.bind` → **旧 pair 销毁**"；变更日志 v1.0.2 ⑥（`00:189`）声称已"明确只有三个触发条件" | 照三条写实现的人会在第二次 `pair.bind` 时保留旧 pair（或拒绝绑定），直接违反"每 agent 至多 1 个 pair"，且两条规则冲突时无优先级。**修法**：把替换列为第四个触发条件，或改写 `00:107` 为"以下四个条件" |
| M-14 | `PAIR_LIMIT` 的**作用域未定义**（契约未同步 02 的实例级 256） | 契约 §4.3（`00:143`"`PAIR_LIMIT`｜**实例**已达 pair 上限｜指数退避后重试"）vs `00:128`（每 agent 至多 1 pair → 替换）；`00:105` 又用"插件**实例**"指代插件实例，故"实例"一词在契约内歧义 | **初版写成"给出相反结果"过重**：02 §3.3（`02:105`）已把上限定义为实例级 256，按该读法两条规则并不冲突，真正缺的只是契约侧的措辞与作用域。风险仍在：若实现者按"每 agent 1 pair"理解 `PAIR_LIMIT`，手机 B 会收到"服务繁忙、请退避重试"并永久退避，两台手机都无法配对。**修法**：契约 §4.3 写明 `PAIR_LIMIT` 是**实例级（256）**，与"每 agent 至多 1 pair → 替换"分开表述 |
| M-15 | `bye` 语义不完整：无 ACK、对端离线时无规则、发送方是否需拆 PeerConnection 未规定 | 契约 §4.1（`00:98`）、§4.3（`00:99`，`t` 为封闭集合且无 `bye.ok`） | ① 无 ACK → 撤销不可确认。**机制是"`bye` 可能丢"，不是"服务端不认 App 的 bye"**（契约 §4.1 `00:98` 明写"任一端发出即立即销毁"；**初版此处的机制表述有误，据作者反驳修正**）：App 发 `bye` 后立刻断 socket，若该帧未被处理，pair 依旧存在（此时四条销毁触发条件都不会被激活——agent 仍在线），而 App 已清掉本地配对（`00:142`）→ 下次连上得 `hello.ok{pair 非空}` 而**静默恢复用户以为已撤销的配对**。对"显式撤销"原语这是真实的持久性缺陷，但修法必须是"**加 `bye.ok` 让撤销可确认**"，而不是去改"服务端是否处理 App 的 bye"。② 对端离线时 Bridge 是转发还是吞掉、丢弃还是排队，未定义。③ 未要求发送方同时拆掉 PC/DataChannel → 可能出现"已解除配对但 DTLS 通道仍活"。**修法**：明确"Bridge 消费 `bye` 而不转发，并回 `bye.ok`"（或明确不需要 ACK 及理由）；规定离线对端的行为；规定发送方必须同时销毁 PC/DC |
| M-16 | 2 s offer 窗口可把"刚重建的 answerer"饿死，且无重触发机制 | 契约 §4.1（`00:94`，2 s 约束只绑定"两次**主动重发**之间"）vs 02 §3.2（`02:92`，实现为"对同一 pair 的 `punch.offer` 做 ≥2s 最小间隔限流，**窗口内重复直接丢弃**"）vs 03 §3.3（`03:85` App 忽略窗口内重复 offer） | 场景：手机与 PC 同时切网（同一 Wi-Fi 掉线，很常见）→ 插件 t=0 主动重发 offer，手机侧网络回调也触发 `RECONNECT`（`03:129`）并重建 PC、t=0.5s 发 `punch.request` → 插件据此产生的 offer 在 t≈1s 被 Bridge 丢弃且被 App 忽略 → 手机侧有一个**已重建、等不到 offer** 的 PeerConnection，而文档没有定义任何"`punch.request` 之后保证来一个 offer"或"App 超时重发 punch.request"的机制。**修法**：明确"由 `punch.request` 触发的 offer 不受 2 s 窗口约束"，或给 App 定义"未获 offer → 退避重发 punch.request（≥2s）" |
| M-17 | 01 的背压机制在 werift 上**没有对应 API**，且两端口径不对称 | 01 §4.2（`01:162`"write() 返回 false 时**停止读 DataChannel**"）；werift `RTCDataChannel` 只有 `onMessage`（推送式）、`send()`、`bufferedAmount`、`bufferedAmountLow(Threshold)`（`…/dataChannel.d.ts`） | 无法"先不读"：消息到达即回调。真正可用的只有 ① 用**契约自带的 WINDOW 信用**让对端停发；② 发送侧用 `bufferedAmountLowThreshold` 限速。03 §3.2（`03:67`）没写这条（它写的是"读 socket → 切帧 → 信用记账"），所以插件照 01 写会发现做不到、App 照 03 写则天然正确。**修法**：01 §4.2 改口径；并补红线"**绝不在 WebRTC 回调线程里阻塞写 socket**"（Android `onMessage` 同样跑在 WebRTC 线程，阻塞会拖垮整个 PeerConnection） |
| M-18 | 03 未写"信令心跳义务"，却要依赖 presence 正确性 | 契约 §4（`00:78`）要求客户端每 **25 s** 发 `ping`、服务端 **60 s** 无帧判离线；02 §3.2（`02:93`）据此广播 `presence{peer_online:false}`；03 §3.3 core-bridge 职责只写"退避重连、消息分发"，§5 无心跳项 | 若 App 不发 ping，Bridge 60 s 后把手机判离线并推给插件；而 03 §3.3（`03:81`）的 `PEER_OFFLINE` 分支又要求"等 `presence` 恢复后自动重试 punch.request" → 在线却被判离线，逻辑自相矛盾。**修法**：03 补 25 s 心跳与 60 s 失效重连，§5 补一项测试 |
| M-19 | launch token 注入方式写了三种，其中"环境变量"这条**原理上不可能成立** | 01 §6（`01:213`"以 `DSML_LAUNCH_TOKEN` 环境变量或控制台粘贴命令注入"）；01 §4.3（`01:171`"命令行参数/**粘贴文件**"）与红线"不落盘"（`01:214`）冲突；契约 §2（`00:38`）"不落盘" | 该 token 由 DSH 进程启动后生成、只打印在 `dsh web:` 行上（源码 `…/dsh-client-connection/lib/index.js:204, 217-223, 347-354`：`WeakMap` 内存持有、仅 `authenticatedUrl()` 会把它拼进 URL）。用户要么在启动前设环境变量（无从知道值），要么带着它重启 DSH（新进程生成**新** token，旧值失效）。**修法（顺带省掉手工拷贝）**：插件 `inject` 加 `connection`（web profile 必有该行），直接 `ctx.connection.authenticatedUrl(\`http://127.0.0.1:${port}\`)` 取出 `token` 参数写入 QR——全程在内存内，不落盘、不需用户拷贝、每次装配取当次进程的 token（API 已核实：`…/dsh-client-connection/lib/index.js:538-541`、`:347-354`） |

| M-20 | **10 分钟宽限期 ⇒ "PC 侧离线超过 10 分钟就必须重新扫码"**，与"手机可长期离线"不对称，且全程没被当作产品决策讨论 | 契约 §4.2（`00:111`"插件断开后宽限期 T = 10 分钟内未以相同 `boot_id` 重连 → 销毁（**兜底清理**）"）、`00:115`（手机可长期离线）；02 §2.1（`02:51`"启动/沿用 T=10min 计时器"） | 笔记本合盖、断网、切 VPN 超过 10 分钟后，**即使 DSH 进程从未重启**，插件以同一 `boot_id` 回来也拿不到 pair → 插件按 01 §4.4 重印新 QR、用户必须重新扫码。该事件比"DSH 重启"频繁得多，而两份文档都只用"兜底清理"的口径描述它，用户此时看到的文案还是"重新扫码"（不是"电脑上的 DSH 已重启"）。**修法**：三选一——① 拉长 T；② 把 pair 持久化（含 Bridge 侧重启后恢复）；③ 明确写下"PC 侧假设常开"这一前提并同步 UX 文案。与 m-9（计时器"沿用"语义）一并改。**r3 补充（承作者主张）**：既然判据已经是 `boot_id`，**T 不该再承担语义、只该承担"内存上限"**，故建议 T 提到 **24 小时**（同进程回归永远免扫码），由 pair 上限 256 封顶。两点联立必须一起定：① T 拉长会让"离线未过期"的 pair 占用配额，故**淘汰策略应为 LRU（先淘汰离线最久者），而不是对新人回 `PAIR_LIMIT`**；② 契约必须显式写出"**PC 侧离线超过 T ⇒ 必须重新扫码**"这一产品含义，并把 T 的值交回用户决策（本报告不代选）。 |

### 2.3 MINOR（24 条）

| ID | 问题 | 证据 / 修法 |
|---|---|---|
| m-1 | `ver` 出现在**帧类型**表里，其"值 1"与 `OPEN=0x01` 同列冲突 | 契约 §3.1（`00:62`）；虽 `ver` 是帧头第 1 字节、实现者通常不会误读，但同列出现 `1` 与 `0x01` 两种写法值得清理（挪出该表） |
| m-2 | 无 OPEN 成功/失败信号 | 插件 `net.connect` 可失败（DSH 正在重启、端口变了），但无失败上报；唯一候选 `RST` 的"原因串"是可选的且无格式（`00:58`）→ App 的 HTTP 请求只能等到自身超时 |
| m-3 | PING/PONG 的 `stream_id`、长度校验、未匹配 PONG 均未定义 | 固定头总带 `stream_id`，但未规定保活帧 MUST 用 0、接收方 MUST 忽略；对"校验 stream_id"的实现会把自己的保活 RST 掉（`00:60-61`） |
| m-4 | 无流释放规则，半关闭流存活至隧道销毁 | 表项小幅泄漏；`OPEN` 复用已在表中的 id 无处置（应 RST）（`00:66`, `00:71`） |
| m-5 | §6 的比对表述漏了"去掉算法前缀"，重复了变更日志①声称已修的那类缺陷 | `00:36` 已定义 `fp` = 去掉算法前缀的值，但 `00:171` 只说"取那一行 `a=fingerprint` 与 QR 中 `fp` 比对"；字面实现会比较 `"sha-256 AB:CD…"` 与 `"AB:CD…"` → 永不相等 |
| m-6 | 多行 sha-256 / session 级与 media 级的优先级未定 | `00:171` 暗示 session 与 media 级都可取，但未说两处 sha-256 不一致时谁胜、也未把 pin 绑定到承载 DataChannel 的 m-line；单 m-line 场景风险低 |
| m-7 | §6"不参与校验的字段"表述过宽 | `00:175` 说 `agent_id`"仅供显示与日志"，但 `00:85`/`00:110` 显示 Bridge 确实用它作设备标识与重启判定键；`00:90` 的"不参与任何 pin 或安全校验"才是准确说法——否则实现者可能干脆不在 pair 记录里存 `agent_id`（而 02 §2 是存的） |
| m-8 | `AGENT_RESTARTED` 在若干可达状态下无法送达；触发条件 2/3 的优先级未定 | `error` 是 Bridge→单（`00:99`）需有活连接。**离线情形**：触发条件 3 先到期则 pair 已不在，手机回来发 hello 只得到 `hello.ok{pair:null}`（`00:127`），`00:140` 的 UX 提示被跳过。**追加（承作者反驳补入）**：若手机 **WSS 仍在线**，它不会重发 hello，则契约承诺的"手机下次 hello 收 pair:null"永不触发，手机停在"PC 离线"；直到对端回来、presence 驱动自动重试 `punch.request` 才拿到 `PAIR_NOT_FOUND`（`03:81`）。故"永久卡住"过重，真实缺陷是**诊断延迟**（提示从"DSH 已重启"退化为"重新扫码"，且要等对端回来才出现） |
| m-9 | 宽限期计时器的**重启算规则**未写 | `00:111` 字面是"每次断开起算"；02 §2.1（`02:51`）写"启动/**沿用** T=10min 计时器"，"沿用"会让 10 分钟内抖动 20 次的插件被误杀。应写明"每次断开重新起算，重连即取消" |
| m-10 | `pendingTokens` 是未定义名词；销毁后未过期 QR 能否再绑定未定；`pair.ready.ttl` 无类型且与 §2 的 5 min 重复授权 | `00:127` 是 `pendingTokens` 唯一出现处；规范词汇是 `ptok_hash`（`00:88`）与 `pt`（`00:35`）；只有宽限期路径被写明清除，`bye`/替换后是否仍可绑定未定 |
| m-11 | coturn 引文**裁掉了限定条件** | `00:154` 引文属实，但原文续句为"…while the session is valid and unexpired…Once the session got disconnected, the client will have to use the new password to re-connect"；宜补"只要 allocation 未被回收（客户端持续 Refresh；coturn 默认 `max-allocate-lifetime` 3600 s）"。两端的 WebRTC 栈会自动 Refresh，故不宜写成"永不失效" |
| m-12 | `relay.alloc`"与 pair.ok 同构"有歧义；两端的凭证是**同一个 coturn 用户** | `00:97` 的 `turn:{...}` 与 `00:152` 的 `urs/username/credential/ttl` 应只写一次；因 `username="<expire>:<pair_id>"` 对两端相同，02 §4 宜注明 `user-quota` 若设置须 ≥2（当前未设，采用 coturn 默认不限，故仅需一句说明） |
| m-13 | nonce 去重窗口无参数（且是安全参数）；hello 超时在契约里写成"建议 10s" | `00:85`；去重窗口 MUST ≥ ±60s 接受窗 + 时钟偏移，否则在被淘汰的那段窗口内可重放；02 §3.3 已把 10 s 写成规则，契约宜由"建议"改为 MUST |
| m-14 | `pair_id` 一致性断言失败后的处置未定；插件向**离线手机**主动 `punch.offer` 的行为未定 | `00:94` 说插件可"任意时刻"重发，但未说手机离线时如何处理（隐含丢弃）；`00:139` 只有 `PAIR_NOT_FOUND` 可用，而 `00:79` 的封闭集合又不容新增"非法消息"错误 |
| m-15 | `pair.ready` 无 ACK | `00:88` 注册 token 后 `00:118` 直接出 QR；若注册失败，用户扫到的是一张永远绑不上的 QR，唯一反馈是 `PAIR_NOT_FOUND`（动作还是"提示重新扫码"，即同一张坏 QR） |
| m-16 | 02 的 XFF 论证前提与现代 Caddy 默认行为不符 | `02:101`/`02:168` 断言 Caddy 默认**追加**；官方文档明示默认**忽略入站值**以防伪造。结论"取最右"仍安全，但按文中心智模型去配 `trusted_proxies`（如放进 `private_ranges` 而客户端就在私网）才会真让伪造值生效 |
| m-17 | `DSML_SIGNAL_URL` 形态有歧义 | 契约 §2（`00:34`）把 QR 的 `s` 定义为完整端点 `wss://host/v1/signal`；01 §4.4（`01:177`）写 `wss://<DSML_SIGNAL_URL>/v1/signal`，§7（`01:221`）只说"Bridge 信令地址" → 运维按 `s` 形态填会拼出 `…/v1/signal/v1/signal` |
| m-18 | 容量叙事与产品上限未打通 | 02 §2（`02:39`"2C4G 万级在线无压力"）与 §3.3（`02:105` 上限 256）并列易被读成"能服务万级配对"；应写明"连接数 ≠ 可用配对数；可用配对数 ≈ min(relay 端口数/2, 带宽/单会话速率)" |
| m-19 | `E1` 含义在两处不同 | 01 §1（`01:21`）/03 §5（`03:148`）= 大响应字节保真；02 DoD（`02:20`）却写"…E1 通过"，而 01 §1（`01:20`）把 relay-only 全链路归给 **W2** |
| m-20 | 02 §7 错误码矩阵不全 | `02:184` 缺 `PAIR_REPLACED`（`02:89` 实现了、`03:80` 要处理）与 `INTERNAL`（契约共 8 个码） |
| m-21 | 契约仓库工具清单不一致 | 01 §9/§10（`01:244`, `01:256`）列 fake-phone + fake-bridge；02 §7（`02:184`）列 fake-phone + **fake-agent**。02 的集成测试（双 fake 客户端）确实需要 fake-agent，01 的交付清单漏了 |
| m-22 | 01 §4.1"版本不匹配直接拒绝（无迁移）"略过头 | `01:152`；`defineDomain` 支持 `compatibleVersions`（`…/dsh-storage-domain/lib/index.js:64`），声明相容版本时不拒绝。"无迁移"成立 |
| m-23 | E1 的 sha256 比对需固定 `Accept-Encoding` | web profile 已配 `compression: gzip`（`…/dsh-web-app/cordis.patch.yml`，schema 默认 `none` 被覆盖）→ 两腿编码协商不同会假失败。**利好**：dist 4.36 MiB + 48 个 client.js 3.62 MiB ≈ 8 MiB 原始资源，gzip 后约 2–3 MiB，"≤30 s 可用"与"≤200 MB/天"都还宽松 |
| m-24 | 03 侧缺 pair 生命周期/状态机演练项；03 内部相对路径前缀不一致 | 01 §8 有 E10、02 §7 有 6 项演练，03 §5 无对应项，而 03 §3.3 新增了三个错误分支与"长中断后 reload"；另 `03:4` 用 `../../remote-app-research/`、`03:38/95/149/160` 用 `../remote-app-research/` |

---

## 3. 可行性评估

### 3.1 成立的部分（可据此排期）

- 端到端链路在 DSH 侧的每个接触点都成立（§1 的 24 项）。三处"看起来会卡死"的地方实测无问题：**loopback 围栏不看端口**、**Origin 与 Host 都是 13080 因而同源校验通过**、**cookie 名绑定含端口的 authority**——契约 §1 的整套推导是对的。
- 免扫码重连、`boot_id` 区分"抖动 vs 重启"、`bye` 立即销毁、禁止在 close 回调销毁 pair 等语义，与 DSH 侧 `patchReload: live` 的 reload/dispose 行为对得上（**前提是 B-1 修掉**）。
- werift 支持持久化证书注入（§1.1）→ 契约 §6 的 pin 方案可实现。
- 前端资源规模与 gzip 现状（m-23）使"≤30 s 可用""≤200 MB/天"处于可达成区间。

### 3.2 只能靠 PoC/真机验证的部分（文档均已识别）

| 风险 | 评价 |
|---|---|
| **werift ↔ org.webrtc 互通** | 仍是第一风险，但"证书注入不可行"已排除（§1.1）。建议 W1 验收写成可判定三条：① 指纹 pin 通过；② 非 trickle offer 含 host+srflx+relay；③ 单条有序 DataChannel 双向 16 KiB 帧无误 |
| **WebView 拒绝明文 loopback（E4）** | 无法静态判定；文档已给 go/no-go 变体 B，处理得当 |
| **`io.getstream` AAR 来源/版本** | 文档标【待验证】，正确 |
| **国产 ROM 后台冻结** | Phase 1 只量化不对抗，口径合理 |
| **境内部案 vs 境外部署** | 已列开放项，且是 W2 公网部署前置，必须拍板 |

### 3.3 工作量与关键路径

合计 ≈ **9–10 人周**（插件 2.5–3 + 服务端 2–2.5 + App 3.5 + 契约仓库 3–5 天），不含 N3。

- **`remotedsh-contract` 是被低估的关键路径**：01 明确"未计入本表"，02/03 也没算它，但插件 CI 的"并上线"断言、02 的 fake 客户端、03 的集成测试都依赖它（按 m-21 还需补 fake-agent）。它晚到位，三份方案的验证手段会同时搁浅。
- **W1 必须在最前**，它是插件工时里唯一的不确定项。
- 三语言 + WebRTC + TURN 部署 + 真机矩阵，单人执行偏乐观但不算离谱；关键是把 BLOCKER/MAJOR 在**开工前**修掉。

---

## 4. 建议的修改清单（按性价比排序）

**第一步（BLOCKER，都是小改动、大收益）**
1. **契约 §4.1**：写死 `hello` 被签名串的序列化（`ts` = epoch 秒十进制、`nonce` = 32B→base64url）（B-3）。
2. **契约 §4.2/§5 + 01 §2.2 + 02 §3.2**：`bye` 只在真正进程退出时发 + 加"当前连接代次"条件（B-1）。
3. **契约 §4.2/§5 + 03 §3.3**：任何新 gather 前 MUST 先 `relay.request`；把 §5 的悬空交叉引用接上（B-2）。
4. **03 §3.3**：hello 补 `pub`（B-4）。

**第二步（MAJOR，一次评审可清完）**
5. 契约：补 `flags`（M-5）；定死 16384 边界与 `len` 上限（M-6）；定死 WINDOW 信用数值与方向（M-7）；补并发 stream 上限（M-8）与 mux 流信用预留（M-9）；补 FIN/RST 状态表（M-10）；把"方案 B"写进契约或降低隧道判死阈值（M-11）；补隧道纪元（M-12）；修"三个→四个触发条件"（M-13）；明确 `PAIR_LIMIT` 是实例级（M-14）；补 `bye` 的 ACK/离线/拆 PC 规则（M-15）；补版本错误码（M-4）；补 sha-256 缺失时 fail-closed（M-2）；`fp` 持久化与每次重 pin（M-1）。
6. 01：删/收紧 `DSML_AUTHORITY`（M-3）；重写背压机制 + 加"不得阻塞 WebRTC 线程"红线（M-17）；token 改由 `ctx.connection.authenticatedUrl()` 程序化获取（M-19）。
7. 02/03：明确 2 s 窗口只约束自发重发或给 App 定义 punch.request 重试（M-16）；03 补 25 s 心跳义务（M-18）。

**第三步（MINOR，随实现推进清理）**：m-1～m-24。

**贯穿项**：补齐 `implementation/README.md`、`remote-link-design-v0.4.md`、`remote-app-research/`——不仅为了 E5/W1–W3/指标表可评审，更因为契约把最重要的恢复流程（"方案 B"）定义在那份缺失的 v0.4 里。

---

## 5. 覆盖度声明

**已核对并判定一致**（跨四份文档逐项比对）：端口与容量（13080 authority / 转发目标 = `ctx.webServer.port` 且 3080 兜底 / Bridge 内网 8080 / coturn 3478 udp+tcp、无 `no-tcp`、v1 无 5349 / relay 49160–49999 = 840 / pair 上限 256）；TTL 与定时器（TURN 3600 s 与 `"<expire>:<pair_id>"` + HMAC-SHA1 / pair_token 32B·5 min·单次 / 宽限 10 min / 隧道 15 s PING·45 s / 信令 25 s·60 s / DSH WS 2000 ms×2 / 退避 500 ms→30 s / hello 10 s / ±60 s + nonce 去重 / 20 msg·s⁻¹·conn / 单 IP ≤16 / XFF 最右）；帧与流控（布局 + ver=1 + type 0x01–0x07 + payload ≤16384 + ≤16 KiB 切片 / 256 KiB 初信 / 8 MiB 全局在途（发送侧自限）/ WINDOW ≥64 KiB / stream_id 由手机分配 / OPEN authority 一致性断言 / WS 独占一条流且字节透明；02 的 64 KB 是信令层上限，与 16 KiB 隧道帧不同层）；语义与角色（插件恒为 offerer、手机 answerer 不得 createDataChannel、无 app→agent offer / 单阶段 ICE / 销毁条件且禁止在 close 回调销毁 / 手机断开不销毁 pair / `boot_id` 进程级且非身份不入 QR / 每 agent 至多 1 pair / presence 与 presence.get（含 hello.ok 初值）/ PEER_OFFLINE 处理 / 字符串错误码 + `t` 封闭集合 + presence-only / TURN"续期"语义）；身份与命名（`fp` = DTLS 证书 SHA-256 且多行只取 sha-256 行 / `agent_id` = sha256(pub) 不做 pin / QR 字段 v·s·pt·fp·a·t / token 不落盘不进 pair.ready / cookie 名由 authority 派生、authority 破坏性变更、PC 端口非破坏性 / 版本串 v1.0.2·frame ver=1·QR v=1 / E2 30 分钟 WS / E1 >1 MiB 组合 URL）。**未发现数值冲突。**

**已核对为"非矛盾"**（给出可兼容读法）：`02:24` "Bridge 重启 = 重新配对" vs 契约"只有三个触发条件"（是状态丢失，不是第四个触发条件）；`02:63` 缺 `boot_id` → `AUTH_FAILED`（命名拉伸，且不达 App）；01"销毁整条 PeerConnection" vs 契约/03"整条 DataChannel 销毁重建"（措辞）；02 的 2 s 丢弃 vs 契约的手机忽略（超集执行，真正的问题是 M-16）；`02:105` 与 `02:121` 的端口算术（49999−49160+1=840，/2=420 ≥ 256）；01 §7"v1.0.1 之前"指 01 自身上一版，非契约版本。

**未核对/无法核对**：`remote-link-design-v0.4.md`、`implementation/README.md`（W/E 编号、§7 指标表）、`remote-app-research/*` 三处引用件缺失；E5 定义；真机与 WebRTC 互通等需实跑的结论。

**方法与校准说明**：每条"正确"结论附源码位置，每条"矛盾"附双方原文与行号，均可独立复核；无法静态判定的一律标注"无法判定"。合并两路独立审计时，本报告对以下两处做了修正：① M-11/M-9 的 DSH 心跳终止时刻按源码重算为**约 6 s**（ping 在 t=2s/4s 各记一次，t=6s 终止），非外部审计给出的"约 4 s"；② `ver` 行是否属"帧类型"表（审计列 MAJOR，本报告列为 m-1 MINOR）——因为 `ver` 在帧头布局中位置明确，误读风险真实但有限。

### 对作者反驳的再校准（本报告不完全认同的 3 处）

作者的反驳质量很高，绝大多数成立并已被本报告采纳（见开头"修订 r2"）。以下 3 处本报告不认为可以照其说法接受：

1. **m-8 的"假设不成立"过重。** 作者称"手机的 WSS 还活着时不会重发 hello，所以真实后果是永久卡在 PC 离线"。但这不是对原判定的推翻，而是**新增的一种情形**：手机**离线后回来**时确实会发 hello 并收到 `hello.ok{pair:null}`，原判定在该情形成立。且"永久卡住"不准确——对端回来后 `presence` 会驱动 App 自动重试 `punch.request`（`03:81`）并拿到 `PAIR_NOT_FOUND` → "请重新扫码"。故真实缺陷是**诊断延迟**（提示退化为"重新扫码"，且要等对端回来才出现），不是永久卡死。
2. **"把隧道判死阈值降到内层 WS 预算之下"不是免费的。** 契约 §3.2.5 的判死条件是"**无任何帧**"，而 PING 间隔是 15 s，阈值必须 ≥ 约 3×间隔，否则**空载隧道会被误判死亡**；要拿到 6 s 阈值就得把 PING 压到 ~2 s（保活流量涨约 7.5 倍，走 relay 时计入"≤200 MB/天"配额）。所以更稳的是他在括号里一并给出的另一条路：明确"内层 WS 终止 → 以新 stream_id 重新 OPEN"（已写入 M-11 修法）。
3. **`getFingerprints()` 的"消掉 m-5"说过头。** 它只消除**插件侧生成 `fp` 的计算风险**（确实少一个出错点），并不消除 §6 的手机侧措辞缺陷——手机仍须"去掉算法前缀再比对"（m-5 保留）。另需在 W1 顺手确认 werift 的 `value` 是否就是"冒号分隔大写十六进制"。

> **收敛（r3）**：作者已撤回"消掉 m-5"的说法，并进一步建议契约**不规定 `fp` 的字符格式**、只规定"归一后比较"。方向同意，但**"可选忽略冒号"必须改成强制且精确定义的规范化**（如"取 `hash-func` 之后的部分，删除全部冒号与空白，ASCII 小写后按字节比较"）——若归一方式本身可选，两端仍可能归一成不同结果，等于把刚消灭的歧义重新引入。规范化本身不削弱安全性（删冒号/大小写折叠对十六进制字节串是双射级等价）。

### 三处校准的收敛状态（r3）

| 处 | 作者主张 | 本报告结论 |
|---|---|---|
| `getFingerprints()` 的效力 | 撤回"消掉 m-5"，主张"契约只规定归一后比较" | **接受撤回**；其建议**接受但需收紧**：归一是**强制且精确定义**的，不是"可选" |
| m-8（`AGENT_RESTARTED` 送达） | 我"对了一半"，但依据（presence 会驱动重试）不成立；应收敛为"销毁时无主动通知 → 诊断延迟或无限等待" | **完全认账**：该依据是未经文本支持的推断（pair 销毁后 Bridge 无关联可推），接受"**销毁时必须显式推 `PAIR_NOT_FOUND`**"作为闭合修法 |
| M-11（降判死阈值） | 成本论证不成立（实算 ≈3% 配额）；应改用 WebRTC 状态机作主触发 | **接受**：撤回成本论证；采纳第三种修法，并补"ICE `failed` 受 30 s consent 约束、覆盖不了 HOL、需与 13–26 s 前端窗口联立"三条边界 |

### 流程提醒（回应"是否直接改文档 + 升 v1.0.3"）

同意"改动即记录"这一做法——契约自带带"确认"列的变更日志，本就是它的会签实践，不必另立"增补草案"。但有三点必须守住：

1. **变更日志的"确认"列不得由 AI 填写**（尤其不得写成"用户确认"），应留"待三方会签"。契约 §7 自己规定"**变更须三方确认并升版本号**"，代填就是把会签记录变成伪造记录。
2. 作者写的是"19/20 条 MAJOR 里**我认同的部分**"——**不认同的必须逐条写明理由并留在文档里**（哪怕只写"不采纳：理由 X"）。否则审计轨迹中断，下一轮没人知道哪一条是被有意否决、哪一条是被漏看。
3. **B-1 的修法①与 M-20 耦合**：前者依赖"10 分钟宽限内能恢复"，后者指出"PC 离线 >10 分钟就丢 pair"。热重载后若网络恰未恢复、重连超过 10 分钟，一样要重扫码。两条必须一起改才算闭合。
