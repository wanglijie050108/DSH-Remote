# 2026-09-13-008 · Go 版 Bridge 补做线上测试（用户质询驱动；006 的验证缺口闭环）

| 项 | 内容 |
|---|---|
| 类型 | 代码（无文件改动，验证留档） |
| 触发来源 | 用户质询「你怎么测试的bridge，你真的拿到返回了吗」 |
| 影响版本 | 契约 v1.0.5；remotedsh-bridge 0.1.0 |
| 变更文件 | 无（测试过程未改任何仓库代码） |

## 改了什么
无代码改动。补做的测试与结论：

**006 时的真实覆盖范围（诚实口径）**：
- `go build` / `go vet` / `go test ./...`（auth 消费共享向量单测 + hub 状态机直接方法调用单测）；
- **`dispatch.go` 与 `server.go`（WS 层）当时一行都没被真正执行过**；此前的全链路 E2E 用的是 Node 版 fake-bridge，不是 Go 版 bridge。

**本次补做的线上测试（真实 Go 进程 + 真实 WS 客户端）**：
1. 启动 `cmd/signal`（TURN_SECRET=test-secret，端口 18081），监听确认；
2. 正例探针：Node `ws` 客户端按契约 §4.2 规范化序列化发 `hello{role:agent}` → **9ms 收到 `{"pair":null,"peer_online":false,"t":"hello.ok"}`**——证明 Go 侧跨语言验签（Node 签 / Go VerifyASN1）通过；
3. 负例探针：被签串与 ts 不符 → **返回 `AUTH_FAILED "bad signature"` 且连接关闭**——hello 门禁生效；
4. 全链路 E2E（信令全程经 Go bridge，WebRTC 隧道按设计点对点不过 bridge）：
   - fake-agent `hello` → `pair.ready` → `pair.ready.ok` → 出 QR；
   - fake-phone 扫码 `pair.bind{ptok}` → Go 消费 token、创建 pair → **双端各收 `pair.ok`（pair-1，服务端日志可见 `[hub] pair created: pair-1`）**；
   - `punch.request` → `punch.offer` → `punch.answer` 全部经 Go bridge 转发（pair_id 断言、2s 限流豁免路径均走到）；
   - ICE 直连后隧道回环 **ECHO PASS（50,000 字节）**。

## 为什么 / 依据
02 §7 集成测试项（「双 fake 客户端脚本 hello→pair→punch 全流程」）此前只对 Node fake-bridge 验证过；本次以同一对 fake 工具对 Go 版执行，等价于 02 §7 集成层与 DoD「两端上线互见 / pair.bind → pair.ok」的线上验证。

## 验证
见上，三项全部 PASS，全程无一次 Go 侧代码修补（wire 行为与单测预期一致）。

## 回溯线索
- 前置：006（Go 实现与单测）；Go 版尚未在线上验证的项：RATE_LIMITED、PAIR_LIMIT（256 容量需注入）、hello 10s 超时、sweeper 60s 判离线、单 IP ≤16——这些留在部署演练（E5）用自动化脚本复现。
