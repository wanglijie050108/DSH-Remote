# 2026-09-13-001 · 方案基线回溯（本目录建立前的全部方案演进）

| 项 | 内容 |
|---|---|
| 类型 | 决策留档（无文件改动） |
| 触发来源 | 建立 changelog 目录，回填既有历史作为回溯基线 |
| 影响版本 | 契约 v1.0.0 → v1.0.5；01/02/03 v1.1 → v1.2 |
| 变更文件 | —（本记录只留档，不改文件） |

## 演进时间线（契约级细节以契约 §7 变更日志为准，此处只记工作区事件）

| 日期 | 事件 |
|---|---|
| 2026-09-09 | 契约 **v1.0.0** 首次冻结（三方）。 |
| 2026-09-09 | 契约 **v1.0.1**：`fp` 重定义为证书指纹、`hello` 补 `ts`/`nonce`、单阶段 ICE、`punch.request` 时序等。 |
| 2026-09-12 | 独立审查（r1→r3 收敛）：4 BLOCKER + 20 MAJOR + 24 MINOR，产出 `审查报告-四文档一致性与可行性.md`。 |
| 2026-09-12 | 契约 **v1.0.2**（用户 2026-09-12 三项决策：宽限期 T=24h、authority 固定 `13080` 不留备用、采用 `boot_id`）；01/02/03 升 v1.1→v1.2；`docs/04`（8 项有意否决留档）、`docs/05`（12 周开发计划）成文。 |
| 2026-09-12 | 契约 **v1.0.3**（第三轮评审收敛：`hello` 规范化序列化写死、双 DataChannel、WINDOW 增量语义、并发 128、未发送队列上限、四销毁条件、pin 归一写死等 20 项，详见契约 §7）。 |
| 2026-09-13 | **第四轮**：`new_docs/` 目录建立（取代 `docs/` 四份），契约 **v1.0.4**（B 组收敛 + launch token 可复用性源码核实 + DataChannel 按 label 识别 + TURN 凭证 TTL 阈值 300s + coturn 限速按服务器 3Mbps 线路取值）；01/02/03 同步（02 DoD 扩至 9 项 pair 生命周期、3Mbps 限速取值）。 |
| 2026-09-13 | **第五轮正文落地**（用户 2026-09-13 对 A1–A5/B1–B4 的批复）：RST 幂等化白名单（A1）、取消 LRU 超限直接拒绝（A2）、每 phone 至多 1 个 pair = 条件⑤（A3）、reload 阈值 10s（A4）、`proto_ver` 升 `"1.0.5"`；A5（presence 驱动重发暂停）与 B2（fail-fast 自救指引）、B3（上游文档回填）按批复不改。**遗留**：变更日志缺 v1.0.5 行、03 的 `proto_ver` 等未同步——由记录 002 修复。 |

## 现状说明（本目录建立时点）

- `docs/`（v1.0.3 时代四份 + 05 计划 + 审查报告）自 `new_docs/` 建立起**被取代但保留未删**，仅作历史；
- `.dsh/skills/dsh-mobile-link/SKILL.md` 的事实索引仍指向 v1.0.3 旧口径，属已知待办（用户批复 B3：开发时自行调整）；
- A1–A5/B1–B4 的逐条批复内容与"改/不改"结论，以记录 002"为什么/依据"栏为准。

## 回溯线索

- 前置：无（本目录首条）。
- 后续：记录 002、003。
