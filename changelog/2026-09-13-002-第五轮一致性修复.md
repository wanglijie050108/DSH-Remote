# 2026-09-13-002 · 第五轮一致性修复（7 项遗留问题，10 处编辑）

| 项 | 内容 |
|---|---|
| 类型 | 方案 |
| 触发来源 | 审查发现：第五轮（A1–A5/B1–B4）落地时，契约 ↔ 03 之间同步不全（1 项联调级硬伤 + 3 处口径矛盾 + 1 处治理欠账 + 2 处边界） |
| 影响版本 | 契约 v1.0.5（变更日志补齐）；01/02/03 v1.2 |
| 变更文件 | `new_docs/00-接口契约(2).md`（5 处）、`new_docs/03-APP端实施方案(2).md`（4 处）、`new_docs/01-DSH端插件实施方案(2).md`（1 处） |

## 改了什么

| # | 文件 | 位置 | 旧 → 新 |
|---|---|---|---|
| 1 | 00 | §2 `t` 字段 | "DSF 侧调研" → "DSH 侧调研"（笔误） |
| 2 | 00 | §3.3 "对端已 `FIN` 后本端仍收到 `DATA`" 行 | "仅当本地 socket 已完全关闭时才 `RST`" → "属终态流迟到帧 → 静默忽略，不得回 `RST`"（消除白名单之外隐含的第四个 RST 触发条件） |
| 3 | 00 | §3.3 新增一行 | 收到 `OPEN` 但 `stream_id` 无效（=0）或重复（已在流表，无论活跃/终态）→ **静默忽略并计错，不回 `RST`**（回 RST 会误杀仍在工作的既有流）。闭环 `docs/04` §三.8 遗留的"OPEN 复用 stream_id"待补项 |
| 4 | 00 | §4.5 `PAIR_REPLACED` 动作列 | "清本地配对信息；提示重新扫码"一刀切 → **按方向写死**：被动方清配对提示重扫；主动换绑方（本地已绑定更新的 pair）MUST 忽略。原表述按本项目"契约优先"裁决规则会导致实现清掉刚建立的新配对 |
| 5 | 00 | §7 变更日志 | 补**缺失的 v1.0.5 行**（条件⑤/取消 LRU/RST 白名单定稿/重复 OPEN/proto_ver 1.0.5/配套修订/用户批复维持项），消除"正文 v1.0.5、日志止于 v1.0.4 且自称遗留四项暂不修"的自相矛盾 |
| 6 | 03 | §3.3 | `proto_ver` = `"1.0.3"` → `"1.0.5"`（照旧实现 App 必收 `PROTO_VER_UNSUPPORTED`，联调级硬伤） |
| 7 | 03 | §3.3 | `PAIR_LIMIT` 从"`PAIR_LIMIT` / `RATE_LIMITED` / `INTERNAL` → 指数退避"中拆出 → **提示"服务繁忙"，不自动重试**（对齐契约 §4.5 的"超限直接拒绝"） |
| 8 | 03 | §4 状态机 | `任一态 →(PAIR_REPLACED)→ NEED_PAIR（清本地配对信息）` → 补主动换绑方忽略分支（对齐 §3.3） |
| 9 | 03 | §5 测试④ | 补"被动方提示 / 主动换绑忽略"两用例 |
| 10 | 01 | §8 单元 | FIN/RST 状态机测试项补：终态迟到帧静默、收到 `RST` 不回、重复/无效 `OPEN` 忽略（RST 风暴回归用例） |

## 为什么 / 依据

用户 2026-09-13 对 A1–A5/B1–B4 的批复：A1 = RST 白名单三类（超限新 OPEN / 帧头非法·len 超限 / 本地写错误）、终态一律静默、收到 RST 一律不回；A2 = 超限直接拒绝访问并提示繁忙（不做 LRU）；A3 = 一台手机只连一台电脑、连新自动销毁旧；A4 = reload 阈值 10s（<12.5 即可）；B1 = "单次 token"说法应修改；B4 = 统一 TTL 阈值口径；A5/B2/B3 = 不改。**本记录不改变任何批复，只修复该批落地时的机械性同步遗漏。**（批复决策本身的留档见记录 001 第五轮行。）

## 验证

grep 断言全过：正文 `"1.0.3"` 字面量清零；`DSF` 清零；`PAIR_LIMIT` 在契约/02/03 三处口径一致（超限直接拒绝 + 不自动重试）；v1.0.5 变更行存在且与正文逐项对应。

## 回溯线索

- 前置：记录 001（第五轮正文落地）。
- 后续：记录 003（终检在本轮基础上又发现 5 处残留，均为"条件⑤使插件成为收件方"引发的端侧行为缺口）。
