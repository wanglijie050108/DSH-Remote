# 2026-09-18-021 · ECS bridge 上线（补丁版）+ 安全组开通 + secret 核实（T4/T7 阻塞解除）

| 项 | 内容 |
|---|---|
| 类型 | 部署 + 基线核实 |
| 触发来源 | 用户选择 A 路径（ECS 部署 bridge）并完成安全组开通；执行 019 交接事项① |
| 影响版本 | 契约 v1.0.5（无契约变更） |

## 部署内容（114.55.114.12）

| 项 | 状态 |
|---|---|
| Node.js | v18.19.1 + npm 9.2.0（apt 安装） |
| fake-bridge | `/opt/dsh-bridge`（**018 补丁版**，blob 01fa9215：复用 `js/turn.mjs` + `TURN_HOST`），`ws@8.21.3` 服务器侧安装 |
| TURN secret | `/etc/dsh-signal.env`（0600），就地读自 `/etc/turnserver.conf`；**SECRET_MATCH**（与 coturn `static-auth-secret` 哈希比对一致，值未回显/未落日志） |
| TURN_HOST | `114.55.114.12` 已写入 env，启动日志 `turn_host=114.55.114.12` 确认（019 交接要求的重启时注入） |
| 常驻 | `dsh-signal.service`（systemd，Restart=always，开机自启），active，监听 `*:8080` |
| 验证 | 本机 TCP 可达 + 真实 WS 握手返回 `HTTP/1.1 101 Switching Protocols` |

## 安全组规则检查（用户导出 CSV 核对）

| 规则 | 结论 |
|---|---|
| 8080/TCP 入方向 | **已生效**（PC 探测 + WS 握手双证）；**但授权对象为 `0.0.0.0/0`，非约定的开发者 /32**——违反 05 §4.1「8080 仅开发者出口 IP」纪律：明文信令对全网暴露 + bridge「单 IP ≤16 连接」限流退化为全站 16。成因疑似当晚出口 IP 轮动（同晚先后实测 `113.200.174.117` / `103.151.172.97`）。**处置建议（待用户拍板，不阻塞测试）**：开发期短期可接受；对外任何真实使用前必须收窄为 /32（或两条 /32 并接受轮动维护） |
| 3478 TCP+UDP、49160–49999/UDP | 在（010 部署所加，coturn 需对公网，正常） |
| 22/3389/ICMP 0.0.0.0/0 | 系统创建的既有规则（项目外）；3389 对全网开放值得留意，但 22 收窄前需先确认 workbench 通道依赖，本轮不动 |

## 交接事项回执

- **交接①（T4/T7 硬阻塞）**：已解除——补丁版 + turn.mjs + TURN_HOST + secret 核实全部落地；旧版（写死 127.0.0.1）已随重启下线。
- **交接②（docs/03 §4 reload 段）**：同意暂不改，等 T5b 实测结论后一次性修订（本轮未动该文档）。
- **fp 判据更正（019）确认**：App 侧 `verifyPin` 对 QR fp 的消费本来就是「去冒号+小写归一后按字节比较」，与契约 §2 的冒号分隔大写形态兼容，鸿蒙 harness 实现无需改动。

## 回溯线索

- 前置：017（07 方案与基线）、018（TURN_HOST 修复）、019（交接清单）、010（coturn）
- 后置：T3 起阶梯联调（DSH 控制台 QR → 模拟器 App）；安全组收窄决策；T5b 结论后修 docs/03 §4
