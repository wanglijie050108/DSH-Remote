# 2026-09-13-009 · 回填 W1 发现：werift RTCCertificate 第三参枚举形态（01 §3 + SKILL §3/§6）

| 项 | 内容 |
|---|---|
| 类型 | 方案 |
| 触发来源 | 用户批复「先改一」（2026-09-13）；实测证据在 004 记录 |
| 影响版本 | 契约 v1.0.5（**无契约变更**）；01 v1.2 正文补记；SKILL.md 工作区版 |
| 变更文件 | new_docs/01-DSH端插件实施方案(2).md（§3 一处）；.dsh/skills/dsh-mobile-link/SKILL.md（§3 一处 + §6 陷阱表两行） |

## 改了什么
1. **01 §3「已核实」块**：构造器签名细化为 `RTCCertificate(privateKeyPem, certPem, {hash: HashAlgorithm.sha256_4, signature: SignatureAlgorithm.ecdsa_3})`，写明字符串 `'sha-256'` 形态"构造器不报错、DTLS 握手必败"；补记 werift↔werift 回环已验证的事实（非 trickle、双通道、16373B 帧、DOM 风格事件 API）与可复跑的 PoC 路径；"仍需实测"①收窄为**仅剩 org.webrtc（Android）侧**，②③ 不变。
2. **SKILL.md §3**：werift API surface 行同步细化，标注 Verified 2026-09-13 与证据路径；"Still unverified" 仅保留 org.webrtc / disconnected·failed 时机 / bufferedAmount 三条。
3. **SKILL.md §6 陷阱表新增两行**：字符串第三参陷阱（过 code review 但上线必败）；裸函数赋值给 werift Event 属性的运行时报错。

## 为什么 / 依据
- 01 §3 原表述"已核实（可排除的风险）"对构造器存在性成立，但对"可实现"的成立条件（枚举形态）未写——照文档直写字符串会在 DTLS 握手阶段以无关报错卡死（004 记录的 W1 实测，`scripts/poc-werift-loopback.mjs` 可复跑）；
- SKILL 存在意义是阻止重复推导（SKILL 开篇），此坑已由本次开发实付成本，必须落档；
- 契约 § 未动 → 无契约版本升级；惯例依据 002/003（new_docs 活文档线配 changelog 编辑）。

## 验证
- PoC 复跑：`node remotedsh-contract/scripts/poc-werift-loopback.mjs` 三判据全绿（证书注入 + 8 候选非 trickle + 双通道 16373B 帧）；
- 引用一致性：`lib/keys.js`、`tools/fake-agent.mjs` 注释与文档表述一致。

## 回溯线索
- 前置：004（W1 发现原始记录）、005（插件实现依赖此形态）；
- 后续：G1（org.webrtc 真机互通）结果出来后，§3 "Still unverified" 首条应销项。
