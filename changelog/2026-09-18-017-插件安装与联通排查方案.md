# 2026-09-18-017 · 插件安装测试与联通链路排查方案（模拟器对接前置）

| 项 | 内容 |
|---|---|
| 类型 | 方案 + 基线核实 |
| 触发来源 | harness App 已装入鸿蒙模拟器；用户要求确认插件 git 同步并输出安装测试与链路排查方案 |
| 影响版本 | 契约 v1.0.5（无契约变更） |

## 基线核实结论（关键新发现）

| # | 结论 |
|---|---|
| F1 | `dsh-mobile-link` 与 origin/main **完全同步**（HEAD==origin/main==8755d6e，插件目录零改动）；远端新增分支 `qc_android_wiring`（不在 main） |
| F3 | 插件本地**依赖未安装**（node_modules 不存在）——link: 安装前必须 `npm install` |
| F4 | **线上 bridge 未部署**：ECS 8080 无监听、无服务单元；`remotedsh-bridge` 缺 `package main`（Dockerfile 期望的 `cmd/`、`vendor/` 不存在）——008 为临时测试未留部署物；桥接兜底 = 契约 `tools/fake-bridge.mjs`（§4 可执行规格，监听 0.0.0.0） |
| F5 | coturn 正常（3478 可达）；当前开发者出口 IP `113.200.174.117`（8080 白名单需含此值，05 §4.1） |
| F2 | 本机安装条件齐备：dsh 0.1.5-rc.2 + pnpm 11.22 + web profile（link: 本地插件先例 `dsh-version-row`） |

## 交付文档

- 新建 `new_docs/07-插件安装测试与联通链路排查方案.md`：
  - **A 插件安装测试方案**：link: 直连仓库安装（装的就是 git 工作树）→ 装配验收判据（`assembled` 日志 / identity.json / fail-fast 对照表+自救）→ 运行验收（QR 形态五查）→ link 模式更新纪律
  - **B 联通链路排查方案**：拓扑与端口表、T0–T7 阶梯测试（T0 bridge 二选一：ECS 部署 fake-bridge / PC 局域网）、三方日志入口（App hilog tag / 插件 `[dsh-mobile-link][*]` / bridge）、12 条症状排查表、06 门禁回填映射

## 待用户决策

- T0 桥接路径选择（A：ECS 部署 fake-bridge（推荐，需安全组 8080 加白 113.200.174.117）；B：PC 局域网（最快，零服务器依赖））
- Go bridge 补 `cmd/main` + systemd 常驻为独立工程项，建议 H2 稳定化时另立 changelog

## 回溯线索

- 前置：016（harness App）、008（Go bridge 行为验证）、010（coturn）
- 后置：A 章安装执行 → T0 桥接就绪 → 阶梯 T1–T7（模拟器联通 H1/H2 前半）
